package engine;

import org.newdawn.slick.opengl.TextureLoader;

public class Utils {

	public Texture getTexture(String fileName, String type) {
		Texture texture;
		texture = TextureLoader.getTexture(type.toUpperCase(), new InputStream)
	}
	
}
